# Maintainer: Brian Madonna <bmadonnaster@gmail.com>
# Depends on: kde-frameworks-6 plasma-activities libkexiv2 libtiff poppler qca phonon qt6

name=okular
version=26.08.1
release=1
source=(https://download.kde.org/stable/release-service/$version/src/$name-$version.tar.xz)
pkg_build() {
    mkdir -p build || return 1
    cd build || return 1

    SKIP_OPTIONAL='Discount;DjVuLibre;EPub;LibSpectre;LibZip'

    cmake \
        -DCMAKE_INSTALL_PREFIX=/opt/kf6 \
        -DCMAKE_PREFIX_PATH="/opt/qt6;/opt/kf6" \
        -DCMAKE_BUILD_TYPE=Release \
        -DBUILD_TESTING=OFF \
        -DFORCE_NOT_REQUIRED_DEPENDENCIES="$SKIP_OPTIONAL" \
        -Wno-author \
        .. || return 1

    make -j${JOBS:-1} || return 1
    make DESTDIR="$PKG" install || return 1
}
