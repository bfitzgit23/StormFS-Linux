# Description: MPEG1/MPEG2 decoder (includes libmpeg2)
# URL: https://libmpeg2.sourceforge.io/
# Maintainer: Brian Madonna <bmadonnaster@gmail.com>
# Depends on: sdl12-compat

name=libmpeg2
version=0.5.1
release=2

source=(
  https://download.videolan.org/contrib/libmpeg2/$name-$version.tar.gz
)

pkg_build() {
  sed -i 's/static const/static/' libmpeg2/idct_mmx.c || return 1

  ./configure \
    --prefix=/usr \
    --enable-shared \
    --disable-static || return 1

  make -j "${JOBS:-1}" || return 1
  make DESTDIR="$PKG" install || return 1

  install -v -m755 -d \
    "$PKG/usr/share/doc/$name-$version" || return 1

  install -v -m644 \
    README \
    doc/libmpeg2.txt \
    "$PKG/usr/share/doc/$name-$version/" || return 1
}
