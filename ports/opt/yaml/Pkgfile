# Add your top line text here
# Your line to add at the top here
# Depends on: 

name=yaml
version=0.2.5
release=1
source=(https://github.com/${name}/lib${name}/releases/download/${version}/${name}-${version}.tar.gz)
