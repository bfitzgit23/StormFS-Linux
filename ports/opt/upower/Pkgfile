# Depends on: libgudev libusb glib

name=upower
version=v1.90.5
release=1
source=(https://gitlab.freedesktop.org/${name}/${name}/-/archive/${version}/${name}-${version}.tar.bz2)
build_opt=" --prefix=/usr       
      --buildtype=release 
      -Dgtk-doc=false     
      -Dman=false"   
