# Description: Screen locker for Wayland compositors
# URL: https://github.com/swaywm/swaylock
# Maintainer: Brian Madonna <bmadonnaster@gmail.com>
# Depends on: meson wayland wayland-protocols libxkbcommon cairo linux-pam

name=swaylock
version=1.8.6
release=1
source=(https://github.com/swaywm/${name}/archive/refs/tags/v${version}.tar.gz)

pkg_build() {
    meson setup build . \
        --prefix=/usr \
        --buildtype=release \
        --wrap-mode=nodownload \
        -Dpam=enabled \
        -Dgdk-pixbuf=disabled \
        -Dman-pages=disabled || return 1

    meson compile -C build || return 1
    DESTDIR="$PKG" meson install -C build || return 1
}
