# Depends on: json-glib libsoup avahi libnotify modemmanager vala

name=geoclue
version=2.7.1
release=1
source=(https://gitlab.freedesktop.org/${name}/${name}/-/archive/${version}/${name}-${version}.tar.bz2)
build_opt="--prefix=/usr       
            --buildtype=release 
            -Dgtk-doc=false"
