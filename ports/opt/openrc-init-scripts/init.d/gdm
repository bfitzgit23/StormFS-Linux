#!/sbin/openrc-run

description="GNOME Display Manager"

depend() {
	need dbus
	need xdm
}

start() {
	ebegin "Starting GDM"
	start-stop-daemon --start --exec /usr/lib/gdm/gdm
	eend $?
}

stop() {
	ebegin "Stopping GDM"
	start-stop-daemon --stop --exec /usr/lib/gdm/gdm
	eend $?
}
