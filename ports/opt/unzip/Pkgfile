# Description: Decompress utility for zipfiles
# URL: https://infozip.sourceforge.net/UnZip.html
# Maintainer: Brian Madonna <bmadonnaster@gmail.com>

name=unzip
version=6.0
release=9

source=(
    https://downloads.sourceforge.net/sourceforge/infozip/${name}${version//./}.tar.gz
    cumulative.patch
    gcc15.patch
)

pkg_build() {
    sed -i 's/-O3//' unix/configure

    make -f unix/Makefile \
        LOCAL_UNZIP="$CFLAGS -std=gnu17" \
        unzips

    make -f unix/Makefile \
        prefix="$PKG/usr" \
        MANDIR="$PKG/usr/share/man/man1" \
        install

    ln -sf unzip "$PKG/usr/bin/zipinfo"
}

