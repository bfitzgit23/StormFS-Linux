# Depends on: curl gdk-pixbuf libarchive 

name=appstream-glib
version=0.8.3
release=1
source=(http://people.freedesktop.org/~hughsient/${name}/releases/${name}-${version}.tar.xz)
build_opt="--prefix=/usr   
      --buildtype=release 
      -D rpm=false"
