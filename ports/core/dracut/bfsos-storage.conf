# BFSOS distribution storage policy for Dracut.
# Keep installer-supported md personalities available in every initramfs,
# including host-only images and later manual/kernel-package rebuilds.
force_drivers+=" linear raid0 raid1 raid10 raid456 "
