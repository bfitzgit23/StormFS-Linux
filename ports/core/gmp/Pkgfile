# Add your top line text here
# Your line to add at the top here
name=gmp
version=6.3.0
release=1
source=(https://ftpmirror.gnu.org/gmp/gmp-$version.tar.xz)
build_opt="--enable-cxx
--disable-static"
bootstrap_opt="$build_opt
--enable-cxx
--disable-static"
pre_build() {
 sed -i '/long long t1;/,+1s/()/(...)/' configure
}
bootstrap_build() {
sed -i '/long long t1;/,+1s/()/(...)/' configure
./configure --prefix=$TOOLS    \
            --enable-cxx     \
            --disable-static \
            --docdir=$TOOLS/usr/share/doc/gmp-6.3.0
make
make install
}
