# Depends on: glib gobject-introspection libxml2 gtk3 libsoup totem-pl-parster vala

name=grilo
version=0.3.16
release=1
source=(https://download.gnome.org/sources/${name}/${version%.*}/${name}-${version}.tar.xz)
build_opt="--prefix=/usr          
            --buildtype=release    
            -Denable-gtk-doc=false"
