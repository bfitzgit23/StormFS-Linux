# Depends on: glib curl freetype gdk-pixbuf gst-plugins-base libjpeg-turbo libgsf libpng poppler

name=tumbler
version=4.21.0
release=1
source=(https://archive.xfce.org/src/xfce/${name}/${version%.*}/${name}-${version}.tar.bz2)
