# Description: The OpenGL Extension Wrangler Library.
# URL: https://glew.sourceforge.net/
# Maintainer: Brian Madonna <bmadonnaster@gmail.com>
# Depends on: glew glu-32 libXi-32 libXmu-32 mesa-32

name=glew-32
version=2.3.1
release=3
source=(https://downloads.sourceforge.net/project/glew/glew/$version/glew-$version.tgz)

# Custom build required: upstream Make variables differ between build and staged install
pkg_build() {
  (
    set -e
	make \
		CC="${CC:-gcc}" \
		LD="${CC:-gcc}" \
		CFLAGS.EXTRA="$CFLAGS -fPIC" \
		LDFLAGS.EXTRA="$LDFLAGS"

	make \
		LIBDIR="$PKG/usr/lib32" \
		PKGDIR="$PKG/usr/lib32/pkgconfig" \
		GLEW_DEST=$PKG/usr \
		install

	rm -r $PKG/usr/include
  )
}
