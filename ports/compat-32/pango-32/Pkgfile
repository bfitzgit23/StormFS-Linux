# Description: A library for layout and rendering of text
# URL: https://www.gtk.org/
# Maintainer: Brian Madonna <bmadonnaster@gmail.com>
# Depends on: fontconfig-32 freetype-32 fribidi-32 harfbuzz-32 pango libSM-32 libXft-32

name=pango-32
version=1.58.2
release=3
source=(https://download.gnome.org/sources/pango/${version:0:4}/${name%-*}-$version.tar.xz)

build_opt="-D introspection=disabled"

post_build() {

	rm -r $PKG/usr/{bin,include}

	# help2man no option in meson options to disable
	rm -rf $PKG/usr/share
}

pre_build() {
        _bfs_require_compat32_extension 2 || return $?
}
