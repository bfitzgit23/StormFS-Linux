# Description: Portable and efficient API to determine the call-chain of a program
# URL: https://github.com/libunwind/libunwind
# Maintainer: Brian Madonna <bmadonnaster@gmail.com>
# Depends on: libunwind

name=libunwind-32
version=1.8.3
release=3
source=(https://github.com/libunwind/libunwind/releases/download/v$version/libunwind-$version.tar.gz)

# Custom build required: upstream architecture configuration is package-specific
pkg_build() {
  (
    set -e
  autoreconf -fvi || return 1

  ./configure \
    --prefix=/usr \
    --libdir=/usr/lib32 \
    --host=i686-pc-linux-gnu \
    --disable-documentation \
    --disable-tests \
    --disable-static || return 1

  sed -i -e 's/ -shared / -Wl,-O1,--as-needed\0/g' libtool

  make || return 1
  make DESTDIR="$PKG" install || return 1

  if [ -d "$PKG/usr/include" ]; then
    find "$PKG/usr/include" -type f ! -name libunwind-x86.h -delete
  fi

  rm -rf "$PKG/usr/libexec"
  )
}
