# Description: Perl Compatible Regular Expressions
# URL:         https://www.pcre.org
# Maintainer: Brian Madonna <bmadonnaster@gmail.com>

name=libpcre-32
version=8.45
release=3
source=(https://downloads.sourceforge.net/project/pcre/pcre/$version/pcre-$version.tar.bz2)

build_opt="--enable-unicode-properties --enable-pcre16 --enable-pcre32 --enable-jit --disable-pcregrep-libz --disable-pcregrep-libbz2 --disable-pcretest-libreadline"

pre_build() {
        _bfs_require_compat32_extension 2 || return 0
  export CC="${CC:-gcc}"
  export CXX="${CXX:-g++}"
autoreconf -fi
}

post_build() {

    rm -r $PKG/usr/{bin,include,share/man,share}
}
