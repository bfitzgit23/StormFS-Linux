# Depends on: glib gobject-introspection gtk3 libxml2 python3-gobject

name=libpeas
version=1.36.0
release=1
source=(https://download.gnome.org/sources/${name}/${version%.*}/${name}-${version}.tar.xz)
build_opt="--prefix=/usr          
            --buildtype=release    
            --wrap-mode=nofallback"
