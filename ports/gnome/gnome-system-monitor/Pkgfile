# Maintainer: Brian Madonna <bmadonnaster@gmail.com>
# Depends on: adwaita-icon-theme gtkmm itstool libgtop libadwaita librsvg

name=gnome-system-monitor
version=50.0
release=2
source=(https://download.gnome.org/sources/${name}/${version%.*}/${name}-${version}.tar.xz)

pre_build() {
    # Catch2 is only required for the upstream unit tests.
    # Normal BFSOS package builds do not build/run the test suite.
    find . -name meson.build -exec sed -i '/catch2/d' {} +
    sed -i '141,151d' src/meson.build
}
