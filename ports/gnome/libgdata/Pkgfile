# Depends on: libsoup gnome-online-accounts gtk3 json-glib vala gcr glib gobject-introspection uhttpmock

name=libgdata
version=0.18.1
release=1
source=(https://download.gnome.org/sources/${name}/${version%.*}/${name}-${version}.tar.xz)
build_opt="--prefix=/usr              
            --buildtype=release        
            -Dgtk_doc=false            
            -Dalways_build_tests=false"
