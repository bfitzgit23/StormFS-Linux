# Depends on: json-glib libseccomp vala glib gobject-introspection icu libsoup3 python3-gobject sqlite

name=tracker
version=3.7.3
release=1
source=(https://download.gnome.org/sources/${name}/${version%.*}/${name}-${version}.tar.xz)
build_opt="--prefix=/usr       
            --buildtype=release 
            -Dman=false"
