# Depends on: dbus glib gtk3 libhandy libxml2 libxslt vala

name=dconf
version=0.40.0
release=1
source=(https://download.gnome.org/sources/${name}/${version%.*}/${name}-${version}.tar.xz)
build_opt="  --prefix=/usr           
            --buildtype=release     
            -Dbash_completion=false "
