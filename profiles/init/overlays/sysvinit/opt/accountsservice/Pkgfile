# Generated by scripts/generate-init-variants.py for sysvinit.
# The upstream Pkgfile remains the systemd baseline.
# Description: D-Bus interfaces and daemon for querying and manipulating user accounts
# URL: https://www.freedesktop.org/software/accountsservice/
# Maintainer: Brian Madonna <bmadonnaster@gmail.com>
# Depends on: polkit glib gobject-introspection systemd vala
# Optional: gtk-doc xmlto python3-python-dbusmock python3-gobject

name=accountsservice
version=26.27.3
release=2
source=(https://gitlab.freedesktop.org/accountsservice/accountsservice/-/archive/$version/accountsservice-$version.tar.bz2)

pkg_build() {
    sed -i '/^SIMULATED_SYSTEM_LOCALE/s/en_IE.UTF-8/en_HK.iso88591/' tests/test-daemon.py

    meson setup build \
        --prefix=/usr \
        --buildtype=release \
        -Dadmin_group=adm
    meson compile -C build -j "${JOBS:-1}"
    DESTDIR="$PKG" meson install -C build
}
